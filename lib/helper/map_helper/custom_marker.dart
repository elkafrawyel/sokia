class CustomMarker {
  String placeId, name, formattedAddress;
  double lat, lng;
  bool isSelected;

  CustomMarker({
    this.placeId,
    this.name,
    this.formattedAddress,
    this.lat,
    this.lng,
    this.isSelected,
  });
}
